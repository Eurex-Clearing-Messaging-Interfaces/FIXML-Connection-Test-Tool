version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run: mvn clean package
      - run: ls -la /home/circleci/project/target/
      - store_artifacts: 
          path: /home/circleci/project/target/fixml-connection-test-tool.zip